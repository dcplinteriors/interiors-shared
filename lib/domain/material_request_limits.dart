/// Max bill image(s) a supervisor may attach when **closing** an item (at least one is required).
/// Single knob — change freely. Mirrors the backend `MAX_BILL_IMAGES`.
const int kMaxBillImages = 3;
